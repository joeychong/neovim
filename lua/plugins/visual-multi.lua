return {
    -- Allow multi cursor select
    {
        "mg979/vim-visual-multi",
        branch = "master", -- Always use the master branch for latest updates
        lazy = false       -- You can set lazy to true if you want to load it on demand
    }
}
